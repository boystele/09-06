@model IEnumerable<WebApplication2.Models.Funcao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Siglas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Siglas)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FuncaoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FuncaoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FuncaoId })
        </td>
    </tr>
}

</table>
